# Core
flask==3.0.0
flask-cors==4.0.0
flask-socketio==5.3.6
pandas>=2.1.0
numpy>=1.24.0
loguru>=0.7.0
python-dotenv>=1.0.0
requests>=2.31.0

# Scientific & Analysis
scipy>=1.11.0
scikit-learn>=1.3.0
statsmodels>=0.14.0

# Machine Learning (Core)
xgboost>=2.0.0
lightgbm>=4.0.0

# Deep Learning (Optional - fallbacks available if not installed)
# torch>=2.0.0  # Uncomment for PyTorch support
# tensorflow>=2.15.0; sys_platform != "darwin" or platform_machine != "arm64"
# tensorflow-macos>=2.15.0; sys_platform == "darwin" and platform_machine == "arm64"

# Visualization (for reporting)
matplotlib>=3.8.0
plotly>=5.18.0

# Utilities
pytz>=2023.3
schedule>=1.2.0
ccxt>=4.1.0  # For crypto exchange connectivity
ephem>=4.1.5  # For astronomical calculations
tqdm>=4.65.0  # For progress bars in scanner
joblib>=1.3.0  # For model persistence

# Production Trading
yfinance>=0.2.0  # For market data feed
cryptography>=41.0.0  # For secure credential vault

# Optional FIX Protocol (uncomment if needed)
# simplefix>=1.0.0  # For FIX protocol parsing
